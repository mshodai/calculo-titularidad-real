[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "calculo-titularidad-real"
version = "0.1.0"
description = "Calcula la titularidad real de una entidad jurídica según la Ley 10/2010 y el AMLR"

[tool.pytest.ini_options]
pythonpath = ["src"]
